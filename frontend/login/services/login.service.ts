import { IAuthData } from "../../models/Auth";

export const authFirebase =  async (dispatch: any, data: FormData) => {
    // dispatch({
    //     type: ,
    // });
    try {
        console.log(`validate ${data} on firebase auth`);
        // return dispatch({
        //     type: ,
        //     payload: data//data.map((sale: ISale) => {return adaptSale(sale)})
        // })
    } catch (error: any) {
        // return dispatch({
        //     type: ,
        //     payload: error.message
        // })
    }
}
export const authGoogle =  async (dispatch: any, data: FormData) => {
    // dispatch({
    //     type: ,
    // });
    try {
        console.log(`validate ${data} on google auth`);
        // return dispatch({
        //     type: ,
        //     payload: data//data.map((sale: ISale) => {return adaptSale(sale)})
        // })
    } catch (error: any) {
        // return dispatch({
        //     type: ,
        //     payload: error.message
        // })
    }
}

export const authFacebook =  async (dispatch: any, data: FormData) => {
    // dispatch({
    //     type: ,
    // });
    try {
        console.log(`validate ${data} on facebook auth`);
        // return dispatch({
        //     type: ,
        //     payload: data//data.map((sale: ISale) => {return adaptSale(sale)})
        // })
    } catch (error: any) {
        // return dispatch({
        //     type: ,
        //     payload: error.message
        // })
    }
}

